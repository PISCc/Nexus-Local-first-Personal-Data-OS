# Nexus Organic Memory Field 优化计划

> 版本：2026-09-03
> 目标：在不改变 Nexus 核心能力的前提下，收敛当前实现与最终版 Organic Memory Field 规范之间的差距。

## 1. 当前结论

当前源码已经基本符合最终视觉基线：奶油薄荷背景、深墨蓝正文、钴蓝主要操作、深青色本地状态面板、少量珊瑚色信号、真实 Nexus 产品图标，以及 `query → result → source` 搜索层级均已存在。

仍有以下问题需要处理：

1. Tauri 打包环境无法加载远程 Google Fonts，指定字体会回退到系统字体。
2. 搜索主输入在桌面端为 13px、移动端为 12px，低于可读性基线；路径输入移动端为 11px。
3. 页面标题仍显示旧的“工程基础”，与默认的“全文搜索”页面不一致。
4. 当前工作树包含 Rust 索引、数据库和文件监听逻辑改动，需要确认这些改动是否属于本次视觉迁移范围。

## 2. 不可变约束

- Organic Memory Field 是 Nexus 当前批准的最终视觉与交互基线，不另起一套配色、字体、布局层级或品牌标记。
- 搜索仍是界面主轴，始终清楚表达 `query → result → source`。
- 保留现有 Tauri 命令、Rust/TypeScript 数据结构、搜索与索引逻辑、路由、表单字段、测试选择器和错误处理。
- 只做视觉、可访问性、响应式和必要交互反馈优化；不做无关重构。
- 不引入紫粉蓝 AI 渐变、赛博霓虹、Emoji 图标、过度毛玻璃、整页卡片堆叠或云端处理承诺。
- 不添加虚构的客户、价格、数据或搜索结果；所有演示内容必须明确为静态示意或来自真实本地状态。
- 不上传用户文件、文件名、内容、向量、元数据或搜索历史。
- 新依赖、新数据模型、公共接口、并发架构和安全/隐私行为必须先获得明确批准。

## 3. 优先级与执行计划

### P0：划定本次迁移的变更边界

**目的**：避免视觉迁移混入未经批准的后端行为变化。

**检查范围**：

- `apps/desktop/src/App.tsx`
- `apps/desktop/src/SearchView.tsx`
- `apps/desktop/src/index.css`
- `apps/desktop/index.html`
- `apps/desktop/public/`
- `apps/desktop/src-tauri/`
- `crates/nexus-core/`
- `crates/nexus-db/`

**执行方式**：

1. 将 UI、测试、文档、品牌资源与 Rust/数据库/监听器改动分组。
2. 对 `get_source_config`、`get_index_health` 以及索引/监听器变化逐项标注“必要反馈”或“独立功能改动”。
3. 只有确认为必要反馈的变化才保留在本次迁移；其余变化拆分为独立任务，不在本计划中回滚或重写。

**验收标准**：

- 原有 Tauri 命令和调用参数未删除或改名。
- 搜索响应字段、索引请求字段、表单字段、路由/页面切换和测试选择器保持兼容。
- 视觉迁移的差异可以单独审阅，后端行为变化有明确记录和批准依据。

### P0：改为本地字体加载

**目标文件**：

- `apps/desktop/src/index.css`
- `apps/desktop/public/fonts/`
- `apps/desktop/src-tauri/tauri.conf.json`

**执行内容**：

1. 移除 `https://fonts.googleapis.com` 的 CSS `@import`。
2. 使用具有明确许可的本地字体资源，通过 `@font-face` 提供：
   - Poppins：标题字重 500/600/700。
   - DM Sans：正文与界面字重 400/500/600/700。
   - IBM Plex Mono：路径、查询和状态标签字重 400/500/600/700。
3. 保留当前 CSS 变量命名和系统回退链，避免改变组件层级。
4. 确认 CSP 继续只允许本地字体，不为字体加载开放外部网络。

**验收标准**：

- 断网启动 Tauri 应用时，三套指定字体仍可用。
- 构建产物不含 Google Fonts 外部 `@import`。
- 浏览器/桌面运行时没有外部字体请求或 CSP 字体阻断错误。
- 字体资源许可、文件体积和加载字重有记录。

> 说明：字体文件属于新增资产，实施前需确认来源和许可证；不为了字体引入新的运行时依赖。

### P1：提升搜索主轴的输入可读性

**目标文件**：

- `apps/desktop/src/index.css`
- `apps/desktop/src/SearchView.tsx`
- `apps/desktop/src/App.tsx`（仅在需要补充可访问性关联时修改）

**执行内容**：

1. 搜索查询输入在桌面和移动端均保持至少 16px。
2. 路径输入、路径结果和状态标签继续使用 IBM Plex Mono，但避免移动端低于可读性下限；建议路径输入不低于 13–14px，辅助标签可保留更小字号。
3. 保持现有输入高度、主按钮高度和禁用/加载状态，不改变 `id`、`name`、`aria-describedby`、提交逻辑或测试选择器。
4. 重新检查窄屏下的换行、按钮布局和浏览器自动缩放行为。

**验收标准**：

- 375px 和 390px 宽度下，搜索输入文字清晰且不会触发不必要的页面缩放。
- 搜索主按钮和取消按钮触控区域至少 44×44px，间距至少 8px。
- 搜索输入仍然能通过键盘提交，加载、取消、错误和无结果状态不回归。
- `query → result → source` 的层级和视觉重点不变。

### P1：修正页面标题与视图语义

**目标文件**：

- `apps/desktop/index.html`
- `apps/desktop/src/App.tsx`

**执行内容**：

1. 将初始页面标题从“工程基础”改为与默认搜索视图一致的标题。
2. 在不新增路由的前提下，根据现有 `activeView` 更新窗口/文档标题：
   - 搜索视图：`Nexus — 全文搜索`
   - 索引视图：`Nexus — 文件索引`
3. 保持现有导航状态、页面标题、可见面包屑和 `aria-current` 一致。

**验收标准**：

- 首次打开应用时标题不再显示旧的“工程基础”。
- 在“全文搜索”和“文件索引”之间切换时，标题与当前视图同步。
- 不引入新的路由库或改变现有页面切换方式。

### P2：补充低成本可访问性反馈

**目标文件**：

- `apps/desktop/src/App.tsx`
- `apps/desktop/src/SearchView.tsx`
- `apps/desktop/src/index.css`

**执行内容**：

1. 增加键盘用户可使用的“跳到主要内容”入口，并确保跳转后焦点可见。
2. 检查搜索错误、索引错误、加载状态和取消结果是否仍通过可读文本与 `aria-live` 传达，而不是只依赖颜色或动画。
3. 检查焦点环在奶油背景、深青面板和结果列表上的对比度与可见性。
4. 保留真实图标的装饰性 `aria-hidden` 设置，不使用 Emoji 代替产品图标或操作图标。

**验收标准**：

- 仅用键盘即可到达导航、搜索、结果、索引表单和取消操作。
- 焦点不会被固定区域或响应式布局遮挡。
- `prefers-reduced-motion: reduce` 下不会出现持续动画或强制平滑滚动。
- 错误信息靠近相关操作，并能被辅助技术感知。

## 4. 验证矩阵

每个实施批次完成后执行：

```text
pnpm run typecheck
pnpm run lint
pnpm run format
pnpm run test
pnpm run build
git diff --check
```

并进行桌面端浏览器/Tauri 冒烟检查：

| 检查项 | 视口/条件 | 通过标准 |
| --- | --- | --- |
| 搜索主轴 | 1440×900、1024×768、768×1024、390×844、375×812 | `query → result → source` 可见，无横向滚动 |
| 键盘操作 | 无鼠标 | 焦点顺序合理，焦点环可见，表单可提交 |
| 状态反馈 | 未配置、索引中、成功、失败、取消、无结果 | 文本、颜色、`aria-live` 语义一致 |
| 字体加载 | Tauri 断网启动 | 指定字体可用，无外部字体请求 |
| 动效 | `prefers-reduced-motion: reduce` | 动画和过渡降级，滚动行为为 auto |
| 数据边界 | 真实本地目录与错误路径 | 不上传数据，不崩溃，错误上下文保留 |

## 5. 暂不处理

- 不重做 Organic Memory Field 的颜色、Logo、信息架构或搜索结果结构。
- 不引入 AI 搜索、语义检索替换、云同步、账号、价格页或虚构演示数据。
- 不新增路由框架、组件库、状态管理库或字体运行时依赖。
- 不借机重构 Rust 索引器、数据库 schema、文件监听并发模型或 Tauri 公共接口。
- 不为了视觉统一修改与本次 UI/反馈无关的业务逻辑。

## 6. 完成定义

本计划完成时，应同时满足：

- 指定视觉基线在离线 Tauri 环境中真实生效。
- 搜索依旧是默认且最清晰的产品入口，并可追溯到来源。
- 主要输入、按钮、焦点、错误、加载和无结果状态符合可访问性与响应式要求。
- 原有核心命令、数据结构、搜索/索引逻辑、字段和测试选择器没有回归。
- 所有验证命令有实际执行记录，且没有已知的严重回归。
- 视觉变更与后端功能变更边界清晰，未批准的架构变化不被并入本次优化。
