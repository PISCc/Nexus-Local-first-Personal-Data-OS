# Nexus --- Project Brief

## 项目定位

Nexus 是一个 **Local-first Personal Data OS（本地优先个人信息中枢）**。

目标不是做一个简单的 AI/RAG
Demo，而是做一个可以长期迭代的中大型软件系统：持续索引用户本地文件、代码、论文、网页等个人信息，提供可靠的全文搜索、语义搜索、AI
问答，最终支持 Agent 在个人数据上执行复杂任务。

## 为什么做这个项目

核心目标是训练"一个人 + Codex 做团队级工程"的能力。

人负责： - 产品方向 - 架构和技术决策 - 需求拆分 - Code Review - 验收 -
理解整个系统

Codex 负责： - 仓库调查 - 实现 - 测试 - Debug - 重构 - 文档 - Review
辅助

原则：**Codex 放大产能，不替代工程判断。**

## 核心产品体验

用户可以： 1. 选择本地目录并建立索引。 2.
按文件名、路径、类型、时间和正文快速搜索。 3.
后续通过自然语言进行语义搜索。 4.
对自己的资料提问，并获得可追溯到原始文件的答案。 5.
查看个人信息/文件活动时间线。 6. 最终让 Agent
调用搜索、读取、关联等工具完成资料整理与研究任务。

## 产品文案要求

所有产品内文案必须具体、简洁，并只描述已经存在或明确标注为规划中的行为。

- 不使用“革命性”“赋能”“开启无限可能”等空泛表达。
- 每个功能说明都要写清楚用户能完成什么；优先使用“输入什么、执行什么、得到什么”的句式。
- 使用自然、像真人表达的语言，避免堆叠术语或宣传腔。
- 避免重复使用“高效、强大、智能、创新”等泛化词汇
- 状态文案要说明当前状态和下一步，例如“索引未完成，请检查目录后重试”，不要只写“出现问题”。
- 演示页中的固定数据必须标明“示例”或“演示”，不能让示例数量、路径或时间看起来像真实用户数据。
- 文案评审时逐条检查：这句话是否说明了用户能做什么、看到什么或下一步做什么；如果不能，就删掉或改写。
- 可以适当使用生动的语句

## 界面与视觉规范

-   **基线状态**：2026-09-03 确认的 Nexus 桌面界面是最终视觉与交互基线，风格名称为 **Organic Memory Field / 有机记忆场**。最终基线固定视觉语言和核心信息层级，但不冻结真实功能、可访问性、响应式适配、必要反馈或缺陷修复。
-   **规范与实现**：修改界面前必须先读 `design-system/nexus/MASTER.md` 和相关页面 override。生产基线以 `apps/desktop/src/index.css`、`apps/desktop/src/App.tsx`、`apps/desktop/src/SearchView.tsx` 为准；`demo/Nexus Visual Direction.html` 是视觉方向参考。
-   **总体方向**：以奶油薄荷纸张感环境、开放留白和少量有机轮廓构成安静、可靠的本地记忆场。界面首先是工具，不是品牌海报；不采用通用 SaaS 仪表盘或整页卡片堆叠。
-   **搜索主轴**：搜索是界面的第一操作和信息主轴，必须清楚表达 `query → result → source`，并让结果能够回到真实来源、路径或原文。视觉层不得削弱确定性搜索和来源可追溯性。
-   **色彩**：奶油薄荷用于背景，深墨蓝用于正文，钴蓝用于搜索与主要操作，深青色用于本地状态与来源面板，珊瑚色只用于少量结果、焦点或注意信号。禁止紫粉蓝 AI 渐变、赛博霓虹和任意新增强调色。
-   **排版**：标题使用 Poppins，正文和操作文案使用 DM Sans，路径、文件类型与状态标签使用 IBM Plex Mono；中文使用本地系统回退。正文基准字号不小于 16px，补充标签不得成为理解操作的唯一方式。
-   **品牌与图标**：必须使用真实 Nexus 产品图标 `apps/desktop/public/nexus-product-icon.png`，保留其原始比例和色彩关系，不重新绘制 Logo。界面图标使用一致的 SVG 语言，不使用 Emoji、库存图片或虚构产品截图。
-   **表面与动效**：卡片只用于确实需要分组的信息；避免过度毛玻璃、模糊、浮层和装饰性光效。动效只承担必要的入场或状态反馈，优先使用 `transform` 与 `opacity`，并响应 `prefers-reduced-motion`。
-   **内容真实性**：不得虚构数据、客户、价格、遥测或云端处理承诺。演示内容必须明确标注为示例或演示，不得伪装成真实用户数据或已实现能力。
-   **功能边界**：纯视觉修改必须保留现有 Tauri 命令、Rust/TypeScript 数据结构、搜索与索引逻辑、路由、表单字段、测试选择器和错误处理。改变视觉语言、语义色、字体角色、核心层级或产品标记前，必须取得明确的人工产品决策并同步更新规范与实现。
-   **可访问性与性能**：所有操作支持键盘并具有清晰焦点；交互目标不小于 44×44px；正文与背景对比度至少 4.5:1；状态不能只靠颜色表达。视觉增强优先使用 CSS、SVG 和现有资源，不为单页引入不必要依赖，不得影响本地搜索响应。

## 长期架构

``` text
Data Sources
    |
Scanner / Connectors
    |
Parsers
    |
Normalized Document Model
    |
    +------------------+
    |                  |
Metadata Store     Search Index
    |                  |
    +--------+---------+
             |
        Query Layer
             |
             UI
             |
       AI / Agent Layer
```

## 第一原则

-   Local-first。
-   核心搜索不依赖 LLM。
-   可靠性 \> 功能数量。
-   搜索质量 \> 炫酷 UI。
-   先做确定性全文搜索，再做 Embedding/LLM。
-   不提前引入云同步、多用户、微服务等复杂度。
-   项目必须可测试、可观测、可维护。
