# Nexus 路线图

## 当前进度

截至 2026-09-02，M0 工程基础、M1 本地文件扫描、M2 内容解析（M2.0–M2.4）、M3 全文搜索、
M4.0–M4.4 增量索引以及 M5.0–M5.4 本地向量混合检索基线已完成。M5 的工程链路已经可用，
但真正的预训练本地模型仍需单独的模型、运行时和质量决策；M6 尚未开始。

| 阶段 | 状态 | 当前结果 |
| --- | --- | --- |
| M0.0–M0.7 | 已完成 | 工程骨架、SQLite、启动状态、测试、CI 和中文文档 |
| M1.0–M1.5 | 已完成 | 本地文件元数据索引、可取消重扫、进度界面和验收记录 |
| M2.0 | 已完成 | 统一 `Document` 模型、来源、标题、正文和位置元数据 |
| M2.1 | 已完成 | 纯文本、Markdown 和代码的 UTF-8 解析、有界读取和安全错误 |
| M2.2 | 已完成 | JSON 原始文本解析、大小和嵌套深度边界 |
| M2.3a | 已完成 | HTML 可见文本解析、过滤和输出大小边界 |
| M2.3b | 已完成 | DOCX 主文档文本解析和 ZIP/XML 资源边界 |
| M2.3c | 已完成 | PDF 文本解析和输入/解压流/输出资源边界 |
| M2.4 | 已完成 | M2 支持格式、失败行为和隐私边界验收 |
| M3.0 | 已完成 | canonical 文档存储、来源追溯和安全读写边界 |
| M3.1 | 已完成 | SQLite FTS5 正文索引和一致性维护 |
| M3.2 | 已完成 | 查询语法和基本过滤器 |
| M3.3 | 已完成 | 确定性 ranking 和匹配片段 |
| M3.4 | 已完成 | 搜索 UI、状态和结果交互 |
| M3.5 | 已完成 | 固定语料、查询集和搜索质量评估 |
| M3.6 | 已完成 | 初始正文索引、端到端搜索验收和边界记录 |
| M4.0 | 已完成 | 基于路径、大小和修改时间的变化判定 |
| M4.1 | 已完成 | 本地文件事件监听、归一化和重扫信号 |
| M4.2 | 已完成 | 事件去重、防抖、稳定性确认和事务批量写入 |
| M4.3 | 已完成 | 后台监听任务、取消、关闭和启动恢复 |
| M4.4 | 已完成 | 创建/修改/删除/移动、原子保存和重启恢复验收 |
| M5.0–M5.4 | 基线完成 | 本地确定性向量、版本化存储、BM25+向量混合检索和离线评估 |

## M0 —— 工程基础（已完成）

目标：建立可长期维护的工程骨架。

范围：- Tauri 桌面应用 - React + TypeScript 界面 - Rust 工作区 - SQLite -
日志与错误处理 - 测试 / lint / format - CI

验收：- 开发模式可以启动桌面应用。- Rust/前端测试可以执行。- 项目可以构建。
- CI 自动执行基础检查。

## M1 —— 本地文件扫描（已完成）

目标：用户指定目录，Nexus 建立文件元数据数据库。

记录：- 路径 - 文件名 - 扩展名 - 大小 - 时间戳 - MIME/类型 - 哈希（按需要）

必须考虑：- 10 万+文件 - 权限拒绝 - 文件扫描期间删除/修改 - 符号链接 -
忽略路径 - 取消 - 进度 - 大文件 - 重复文件

验收：10 万级文件扫描可完成，UI 不被阻塞，单文件错误不终止整个扫描。

## M2 —— 内容解析（已完成）

当前进度：

- M2.0 已完成：定义统一 `Document` 模型，不读取正文、不实现具体解析器。
- M2.1 已完成：支持 txt、Markdown、py、rs、js、ts、java、cpp 等纯文本和代码文件。
- M2.2 已完成：支持 JSON 合法性校验，保留去除 BOM 后的原始文本，并限制文件大小和嵌套深度。
- M2.3a 已完成：支持 HTML/HTM 的 HTML5 容错解析，过滤非内容元素，提取规范化可见文本，
  并限制原始输入和提取后输出大小。
- M2.3b 已完成：支持 DOCX 主文档 XML 的段落、换行、制表符和实体文本提取，并限制 ZIP
  输入、主文档 XML 条目和提取后输出大小。
- M2.3c 已完成：支持 PDF 按逻辑页序提取文本，并限制原始 PDF、单个解压流和提取后
  输出大小；损坏输入和第三方解析器异常均不会泄露正文或拖垮调用方。
- M2.4 已完成：所有当前支持格式都能生成统一 `Document`；不支持或损坏输入返回可分类
  的安全失败；fixture 可重现；没有正文上传或云端依赖。

每种格式的专属位置元数据仍不扩展当前 `Document` 模型。M3.0 已在数据库层建立
canonical 文档存储；本阶段仍未建立 FTS5 索引或查询接口。

统一为 Document 模型：`Source → Parser → Document`

## M3 —— 全文搜索

目标：真正搜索文件正文，而非仅文件名。

M3.0 已完成：`nexus-db` schema 版本升级到 3，新增 `documents` 表，保存不透明文档
ID、本地来源路径、标题、正文和可选行范围；提供按 ID 的 upsert、读取和删除入口，
并通过无损路径键保留到原始文件的追溯能力。M3.0 不建立 FTS5 表，不实现查询语法、
tokenizer、ranking、snippet 或 UI。

M3.1 已完成：在 canonical 文档表之上建立 SQLite FTS5 external-content 正文索引，
使用 `unicode61 remove_diacritics 1`，并通过 triggers 和 migration rebuild 验证索引
与 canonical 文档的一致性。

M3.2 已完成：在 `nexus-db` 中提供受限的关键词、双引号短语、文件名/路径、扩展名、
文件类型和 UTC 日期过滤查询。所有条件按 AND 组合，结果按文档 ID 稳定排序并有数量上限；
非法查询返回安全错误。

M3.3 已完成：使用 SQLite FTS5 BM25 对标题和正文进行确定性 relevance 排序，标题权重高于
正文，相关性相同按文档 ID tie-break；使用纯文本标记返回有限长度 snippet，不生成 HTML，
不依赖 LLM。

M3.4 已完成：桌面应用默认进入全文搜索页，支持显式提交查询、loading、空结果、错误和取消
后的安全状态；结果显示标题、路径、类型、时间、相关性和纯文本 snippet。UI 通过 Tauri 命令
调用搜索和原文件打开能力，不直接访问 SQLite；M1 文件索引页和重扫流程继续保留。

M3.5 已完成：增加仅测试可见的离线评估 harness，固定 10 条代表性文档和 9 个带人工相关性
标注的查询，记录 Recall@3、Top-1 命中、查询中位/p95 延迟、SQLite 文件大小和 FTS5 segment
bytes，并与 M3.2 文档 ID 顺序基线比较。当前 BM25 macro Recall@3 为 0.9722，基线为 0.9444；
没有读取真实用户资料，也没有在评估单元中调整 tokenizer 或权重。

M3.6 已完成：桌面文件索引页通过 `indexContent` 模式把 M2 已验收的支持格式接入
`documents`/FTS，关键词、短语、基本过滤器、BM25、snippet 和原文件打开均形成同一条本地
闭环。单文件解析失败和不支持格式不会终止扫描；数据库写入错误仍会终止任务。核心层原有
metadata-only 重扫接口保持不变；陈旧正文清理、修改检测和 watcher 留给 M4。详见
`docs/acceptance/0015-m3-acceptance.md`。

支持：- 关键词 / 短语 - 文件名/路径 - 扩展名 - 日期过滤 - 排序 - 匹配片段

学习重点：- 倒排索引 - 分词器 - BM25 - 查询解析器

## M4 —— 增量索引

加入文件监听：创建 / 更新 / 删除 / 移动。

M4.0 已完成：核心层可以比较上次和本次文件状态，识别新增、修改、未变化和已消失
的文件。当前只使用路径、大小和修改时间等廉价信息；修改时间缺失时保守地重新处理，
不计算 hash、不读取正文、不写数据库，也不引入 watcher。实时事件接入、去重、防抖、
陈旧正文清理和移动身份识别留给 M4.1–M4.4。

M4.1 已完成：核心层使用 `notify 8.2.0` 递归监听指定目录，并把平台事件转换为统一的
创建、修改、删除、重命名和完整重扫信号。事件只携带路径，不读取正文、不写数据库；监听
范围外路径会被过滤。去重、防抖、稳定性重检、批处理和自动更新留给 M4.2。

M4.2 已完成：核心层以短暂安静窗口合并重复事件，按路径保留最终操作，并在正文解析前后
重新确认文件状态。元数据、canonical 正文和 FTS 更新在一个 SQLite 事务中提交；数据库
事务失败可有限重试，单文件解析失败不会覆盖已有正文。

M4.3 已完成：桌面层用独立后台任务持续消费 watcher 和归并批次，UI 只接收安全状态与统计
事件；取消和应用关闭会停止继续接收事件并等待任务退出。成功完成正文初始索引后保存监听
目录，下一次启动先完整恢复该目录，再重新开启自动同步。

M4.4 已完成：通过真实临时目录和重启模拟验证创建、修改、删除、移动、临时文件原子保存、
重复事件和最终一致性；数据库回滚、损坏文件保留旧正文以及关闭恢复路径均有测试和记录。

重点处理：- 重复事件 - 防抖 - 竞态条件 - 原子保存 - 临时文件 - 取消 / 关闭

## M5 —— 语义搜索

加入 Embedding，但保留全文搜索。

当前状态：M5.0–M5.4 的工程基线已完成。当前默认 provider 是本地确定性特征向量，
只使用标题和正文，不访问网络、不读取模型文件，也不宣称具备预训练语言模型的通用语义能力。

已完成：

- M5.0：固定 provider 边界、模型 ID/版本、256 维输出和标题/正文输入范围。
- M5.1：新增 schema 5 的模型登记、版本化向量存储、输入指纹和更新/删除清理。
- M5.2：保留 SQLite FTS5 lexical 搜索，增加向量候选和 RRF 混合；元数据过滤同时约束两条路径，
  缺少模型或异常向量时回退到 lexical 结果。
- M5.3：全量重建和 M4 增量更新都使用有界批次维护向量；删除、移动、解析失败和取消不会破坏
  已提交的本地索引状态。
- M5.4：复用 M3 固定评估集比较 BM25 与 hybrid；当前 Recall@3 和 Top-1 相同，且 hybrid 有额外
  计算开销，因此没有加入 reranking。

目标架构： `BM25 + Vector Search -> Fusion -> (必要时 Rerank)`

下一步是否采用真正的预训练本地模型，需要先决定模型文件来源、许可、体积、运行时、设备资源、
升级策略和真实语料评估方式。该决定确认前不引入外部模型或云端 API。重点仍是混合搜索质量，
而不是简单的向量数据库演示。

## M6 —— Ask Nexus

自然语言问题：
`Question -> Query Analysis -> Retrieval -> Rerank -> Context -> LLM -> Answer`

硬性要求：回答必须可追溯到原始资料，尽可能提供文件/页码/片段引用。

## M7 —— 个人时间线

建立个人资料活动时间线，并支持按时间检索与总结。

## M8 —— Agent 层

Agent 可以调用：- search_documents - read_document - find_related - search_code -
get_timeline

示例任务： "整理最近半年所有 Agent Memory 资料，分类比较并生成报告。"

## 范围控制

当前里程碑未完成前，不主动实现后续里程碑。M5 基线已完成，但在预训练模型决策和真实语料
评估前不扩展为更大的 AI 平台；M6 及之后仍不实现，尤其不要提前加入 Agent。
